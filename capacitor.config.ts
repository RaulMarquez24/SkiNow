import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.SkiNow.starter',
  appName: 'SkiNow',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
